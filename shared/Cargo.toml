[package]
name = "soundthemed-shared"
version = "0.1.0"
edition = "2024"
description = "Shared library for soundthemed — config, theme resolution, and sound ID constants"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
dirs = "6"
log = "0.4"
